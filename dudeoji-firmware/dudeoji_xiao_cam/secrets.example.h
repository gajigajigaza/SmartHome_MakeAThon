#pragma once

// 나중에 실제 Wi-Fi 정보로 변경 (2.4GHz 대역만 지원)
#define WIFI_SSID "WIFI_NAME"
#define WIFI_PASSWORD "WIFI_PASSWORD"

// http://<MDNS_NAME>.local 로 접속하게 됨. 보드가 여러 대면 각자 다른 이름 사용
#define MDNS_NAME "dudeoji-cam"
