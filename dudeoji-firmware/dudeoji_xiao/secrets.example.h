#pragma once

// 나중에 실제 Wi-Fi 정보로 변경
#define WIFI_SSID "WIFI_NAME"
#define WIFI_PASSWORD "WIFI_PASSWORD"

// 로컬 FastAPI 서버가 실행되는 컴퓨터의 IPv4 주소
// 127.0.0.1을 넣으면 안 됨
#define WS_HOST "192.168.0.10"
#define WS_PORT 8000

// 두더지 웹에서 사용하는 실제 장소 ID로 나중에 변경
#define PLACE_ID 1

// 현재는 컴파일용 임시값
// 실제 로그인 토큰은 GitHub에 올리면 안 됨
#define AUTH_TOKEN "TOKEN_TO_BE_ADDED_LATER"